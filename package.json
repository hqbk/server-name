{
  "name": "server-name",
  "version": "0.1.0",
  "description": "Generate a server name from famous philosophers names",
  "main": "src/server-name.js",
  "bin": {
    "server-name": "./bin/server-name"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Roald Hacquebord <roald@hacquebordbuckles.nl>",
  "license": "MIT",
  "dependencies": {
    "philosophers-names": "^1.2.0",
    "slug": "^0.9.1"
  }
}
